database:
  host: localhost
  port: 5432
  user: amazon_analyst
  password: amazon_analyst_2024
  database: amazon_analyst
  sslmode: prefer
  pool_size: 10
  max_overflow: 20
  pool_timeout: 30
  pool_pre_ping: true
  pool_recycle: 3600
api:
  base_url: https://openapi.sellfox.com
  timeout: 60
  retry_count: 3
  retry_delay: 1
  # 可选：当未在环境变量/.env中配置时可临时提供（不建议提交真实值）
  # client_id: ""
  # client_secret: ""
sync:
  batch_size: 500
  max_history_days: 30
  enable_validation: true
  parallel_workers: 4
